interface:
  display_name: "Secure"
  short_description: "Review, harden, verify production security"
  default_prompt: "Use $secure to scan this whole project, fix confirmed production/security issues, verify, rescan, and report fix coverage."
